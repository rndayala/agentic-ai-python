[project]
name = "agentic-ai-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Raghunath D", email = "rndayala@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openai>=3.3.1",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
agentic-ai-python = "agentic_ai_python:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
